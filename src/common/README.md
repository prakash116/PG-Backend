# Common layer

Use this directory for application-wide concerns shared by multiple feature modules,
such as decorators, exception filters, guards, interceptors, middleware, and pipes.

Keep feature-specific code inside its feature directory under `src/modules`.
